# 変更履歴

## 2026-08-06

- PWAのmanifest、Service Worker、アイコンを追加し、キャッシュをv2へ更新。
- Time RoadへLISTと共通のターゲット並び替えと保存を追加。
- Googleスプレッドシート予定表の `date`、`hours` 列を例外日として取り込む機能を追加。
- ユーザー所有の新しいTimeMarkスプレッドシート／Apps Scriptへ同期先を移行。旧既定URLを保存済みの端末も、新しい同期先へ自動で切り替える。
- Web公開に依存しないパッケージアプリ化の基盤として、Tauri 2のmacOS／iOS／Android／Windows対応プロジェクトを追加。画面資産をアプリに同梱し、パッケージ版ではService Workerを使わない構成へ変更。
- macOSパッケージ設定へTimeMarkアイコンを明示的に登録し、`TimeMark.app` とDMGへ `icon.icns` を同梱するよう修正。
- 利用者向けのApps Script同期設定を廃止し、iPhone／iPad／Macアプリ内のGoogle OAuthとSheets APIで、各利用者が自分用のTimeMarkシートを作成・保存・読込できる構成へ変更。JSONの端末内移行は継続。
- Googleのアクセストークンはメモリ内だけで扱い、通常の端末保存領域へ残さない。iOSのOAuthコールバックURLとTauriの認可セッション権限を追加。
- iOS実機検証で判明したGoogleトークン名の不一致を修正し、`AuthenticationServices` のリンクと自動署名設定をiOSプロジェクトへ追加。
- iPhone／iPadのLISTは既存のドラッグハンドル、Time Roadは既存のターゲット見出しをなぞって並び替えられるようにし、LISTの長押し編集は維持。
- iPhoneアプリで使えないブラウザ標準の確認ダイアログを、アプリ内の確認画面へ置き換えた。シート読込・予定表読込・接続解除は確認後に確実に実行する。
- LIST／Time Roadの並び替えは明示的な大きいグリップだけで開始し、画面端へ持っていくと自動スクロールする。TimeMark画面をGoogleスプレッドシートへ遷移させる方式は取り下げ、「シートを開く」からスプレッドシート／DriveまたはSafariへ外部起動する方式に変更した。
- `SchoolEvents` の `date`、`grade`、`category`、`title`、`source` を学校予定として読み込み・表示できるようにした。`vacation` は手入力例外日がないときだけ0時間として扱い、新規シートには `SchoolEventsExample` の記載例を作成する。
- `SchoolEvents` のみを持つ従来シートも、予定表専用の読み込み元として接続できるようにした。端末バックアップ用の `TimeMarkData` は要求しない。
- MacアプリのWebViewでも、LISTとTime Roadの明示的なグリップをマウスでドラッグして並び替えられるようにした。
- LISTとTime Roadに上下ボタンを追加し、ドラッグできないときもターゲット順を一段ずつ変更できるようにした。
- 個別アーカイブと、目標日後1週間／1か月の自動アーカイブを追加した。アーカイブ済み表示をONにすれば、削除せずに非表示ターゲットを確認・復元できる。
- 自動アーカイブへ目標日後1日を追加し、LISTから開くターゲット画面で個別アーカイブできることを設定に明記した。ターゲット色は13色へ拡張した。
- 個別アーカイブ操作をターゲット編集画面へ追加し、個別に解除したターゲットは自動アーカイブからも除外するようにした。
- アーカイブ済みを表示するときも、通常ターゲットを先に、アーカイブ済みを下へまとめるようにした。
- 設定画面を「自分のTimeMarkデータ」「自分の予定」「学校予定（SchoolEvents）」「Googleスプレッドシート接続」「端末データ」に整理した。自分の予定と学校予定の読込みを分け、接続管理・JSON移行は折りたたんで通常表示を簡潔にした。
- アプリ版LISTへ「全体の学習時間」パネルを追加し、`−`／`＋` で端末ごとに保存される表示サイズ変更を追加した。Google接続ボタンは常時表示へ戻し、アーカイブ済みターゲットはLIST・Time Road最下部の開閉式アーカイブから復元できるようにした。
- 全体学習時間の「いつまで」日付を選べるようにし、アーカイブ内でもLISTの期限超過日数とTime Roadの表示を維持した。

## 2026-08-09

- Androidネイティブプロジェクトを追加し、arm64向けの署名なしAPK・AABをローカル生成できるようにした。初回ビルドに必要なAndroid SDK、NDK、Java 21の組み合わせを確認した。
- Windows GitHub Actionsを追加し、mainへの関連変更時にWindows向けMSI・EXEを自動ビルドして、30日間のビルド成果物として保持するようにした。

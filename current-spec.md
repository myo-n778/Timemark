# TimeMark 現行仕様

## 提供形態

- TimeMarkは端末内へ画面・計算・データ形式を同梱するパッケージアプリとして提供する。最初の配布対象はiPhone、iPad、Macで、同じコード基盤からAndroidとWindowsにも展開する。
- アプリの起動・端末内保存・JSONのインポート／エクスポートはWeb公開やGitHub Pagesに依存しない。
- PWAファイルはブラウザ用フォールバックとして残すが、パッケージアプリではService Workerを登録しない。

## データ

- 端末内データは各OSのアプリWebViewが保持する `localStorage` に保存する。アプリを削除する前にはJSONエクスポートで復旧用バックアップを作成できる。
- Googleスプレッドシート同期はパッケージアプリ内のGoogleログインで行い、利用者がアプリから自分用のTimeMarkシートを作成する。Apps Script URL、共有トークン、管理者設定は不要である。
- 同期先は `TimeMarkData` シートで、端末内バックアップを保存・読込する。予定表読込は `TimeMarkSchedule` の `date`、`hours`、`note` 列を読み、例外日の稼働時間へ反映する。同一日付は上書きする。
- Googleのアクセストークンはメモリ内だけで保持し、端末の通常保存領域へ記録しない。アプリ終了または有効期限後は再接続が必要である。シートID・URLと表示用メールアドレスだけは端末内へ保持する。
- 既存端末で作成済みのTimeMarkシートはURLを指定して再接続できる。任意の既存GoogleスプレッドシートをTimeMark形式へ変換する機能は今回の対象外とする。

## 並び順

- ターゲットの順序は `state.targets` を正本とし、LISTとTime Roadで共通に表示する。
- 両画面でのドラッグ並び替えは保存され、再読み込み後も維持する。

# TimeMark 現行仕様

## 提供形態

- TimeMarkは端末内へ画面・計算・データ形式を同梱するパッケージアプリとして提供する。最初の配布対象はiPhone、iPad、Macで、同じコード基盤からAndroidとWindowsにも展開する。
- アプリの起動・端末内保存・JSONのインポート／エクスポートはWeb公開やGitHub Pagesに依存しない。
- PWAファイルはブラウザ用フォールバックとして残すが、パッケージアプリではService Workerを登録しない。

## データ

- 端末内データは各OSのアプリWebViewが保持する `localStorage` に保存する。アプリを削除する前にはJSONエクスポートで復旧用バックアップを作成できる。
- Googleスプレッドシート同期はパッケージアプリ内のGoogleログインで行い、利用者がアプリから自分用のTimeMarkシートを作成する。Apps Script URL、共有トークン、管理者設定は不要である。
- 同期先は `TimeMarkData` シートで、端末内バックアップを保存・読込する。予定表読込は `TimeMarkSchedule` の `date`、`hours`、`note` 列を読み、例外日の稼働時間へ反映する。同一日付は上書きする。
- `SchoolEvents` は `date`、`grade`、`category`、`title`、`source` 列を持つ学校予定表として読み込み、設定画面に同じ列構成で表示する。`category` が `vacation` の日は、手入力の例外日がない場合だけ0時間にする。記載例は新規シートの `SchoolEventsExample` に用意する。
- Googleのアクセストークンはメモリ内だけで保持し、端末の通常保存領域へ記録しない。アプリ終了または有効期限後は再接続が必要である。シートID・URLと表示用メールアドレスだけは端末内へ保持する。
- 接続済みの間は保存・読込・予定表読込で再ログインを求めない。読込・予定表読込・接続解除の確認は、iPhone／iPadでも確実に表示できるアプリ内画面で行う。「シートを開く」はTimeMarkの画面を遷移させず、iPhone／iPadではスプレッドシート／DriveまたはSafariへ、Macでは既定ブラウザへ渡す。
- 既存端末で作成済みのTimeMarkシートはURLを指定して再接続できる。`SchoolEvents` だけを持つ旧シートも、予定表専用の読み込み元として接続できる。この場合、端末バックアップの保存・読込は利用せず、学校予定の読込だけを行う。Googleログイン時には、TimeMark自身が作成したシートへの保存権限に加え、利用者が指定した既存シートの学校予定を読むための読み取り権限を求める。任意の既存GoogleスプレッドシートをTimeMark形式へ変換する機能は今回の対象外とする。
- ターゲットはターゲット編集から個別にアーカイブできる。設定の自動アーカイブは「しない」「目標日後1日」「目標日後1週間」「目標日後1か月」から選ぶ。個別に解除したターゲットは自動アーカイブ対象からも除外する。アーカイブは削除ではなく、端末保存・JSONバックアップにも保持する。

## 並び順

- ターゲットの順序は `state.targets` を正本とし、LISTとTime Roadで共通に表示する。
- 両画面でのドラッグ並び替えは保存され、再読み込み後も維持する。
- 新規ターゲットは目標日を基準に既存列へ挿入する。ただし既存ターゲット同士の順番は、手動変更済みを含めて変えない。
- アーカイブを通常表示で隠している間も、表示中ターゲットの並び替えは維持できる。非表示ターゲットは削除されず、その相対位置を保つ。
- カラーはオレンジ、ミントグリーン、ブルー、レッド、パープル、イエロー、ライム、ティール、シアン、インディゴ、ピンク、ブラウン、グレーから選択できる。
- iPhone／iPadとMacではLISTとTime Roadの明示的なグリップをドラッグして並び替える。LISTとTime Roadの上下ボタンでも同じ順序を一段ずつ変更できる。ドラッグ中に画面上端・下端へ近づけると、同じ画面を自動スクロールできる。LISTのカード本体の長押し編集は、グリップとは競合しない。

## 設定画面

- 設定画面は「自分の設定」「自分のTimeMarkデータ」「自分の予定」「学校予定（SchoolEvents）」「Googleスプレッドシート接続」「端末データを移行する」に分けて表示する。
- 自分のTimeMarkデータは `TimeMarkData` への保存・読込み、自分の予定は `TimeMarkSchedule` の読込みと端末内例外日の管理、学校予定は `SchoolEvents` の読込み・閲覧として、画面上でも独立して扱う。
- Google接続のURL入力、シート作成・既存シート接続・シートを開く・接続解除は「シートを作成・接続・管理」に折りたたむ。端末間JSON移行も通常は折りたたむ。各操作の処理内容は変更しない。
- 「Googleに接続」はGoogleスプレッドシート接続欄に常時表示し、初回ログインの入口を明確にする。

## LIST・Time Roadの表示

- LISTの最上部には「全体の学習時間」を表示し、基準日から通常表示中の学習ターゲットの最終目標日までの可処分時間と件数を示す。基準日は端末内に保存できる。
- 表示サイズはLIST最上部の `−`／`＋` で小・標準・大・特大から選び、アプリ全体へ反映して端末内に保存する。
- アーカイブ済みターゲットは通常ターゲットの下、LISTとTime Roadそれぞれの開閉式「アーカイブ」内へ置く。設定画面の一括表示切替は置かず、アーカイブ内の「復元」から通常表示へ戻す。

# 回帰チェック

- LISTでドラッグ並び替え、右クリック編集、長押し編集が動く。
- Time Roadでドラッグ並び替えでき、LISTと同じ順序になる。
- iPhone／iPadではLISTとTime Roadの大きいグリップをなぞって並び替えでき、画面端で自動スクロールする。LISTのカード本体を長押しすると編集できる。
- MacアプリではLISTの左グリップとTime Roadの見出しグリップをマウスでドラッグして並び替えできる。
- LISTとTime Roadの上下ボタンは、共通のターゲット順序を一段ずつ変更し、再読み込み後も維持する。
- 個別アーカイブ、目標日後1日／1週間／1か月の自動アーカイブがLIST／Time Roadで共通に反映され、JSONバックアップ後も維持する。
- アーカイブ済みターゲットはLISTとTime Roadの最下部に閉じた「アーカイブ」として表示され、開くと確認・復元できる。通常ターゲットの並び替え対象には混ざらない。
- ターゲット編集から個別アーカイブ・解除ができ、自動アーカイブ対象の個別解除も通常表示を維持する。
- 新規作成・編集で13色の候補を選べる。
- アーカイブを隠した状態でも、表示中ターゲットのドラッグ・上下ボタン並び替えが保存される。
- 再読み込み後もターゲット順序が維持される。
- 設定画面にApps Script URL・共有トークンの入力が表示されず、Google同期とJSON移行が表示・操作できる。
- 設定画面で「自分のTimeMarkデータ」の保存・読込み、「自分の予定」の予定表読込み・例外日管理、「学校予定」の読込み・閲覧が別セクションに表示され、相互に統合されていない。
- GoogleスプレッドシートのURL入力・作成・接続・外部で開く・接続解除、および端末間JSON移行は折りたたみから開いて操作できる。
- LIST最上部の「全体の学習時間」で基準日・可処分時間・学習ターゲット件数を確認でき、`−`／`＋` の表示サイズ変更は再起動後も維持される。
- Googleスプレッドシート接続欄には「Googleに接続」が常時表示される。
- Googleログイン後、新しいTimeMarkシートの作成、保存、読込ができる。アクセストークンが通常保存領域に残らない。
- `TimeMarkSchedule` の予定表読込が例外日へ反映される。
- `SchoolEvents` の `date`、`grade`、`category`、`title`、`source` が設定画面へ表示され、`vacation` の未設定日だけが0時間になる。手入力済みの例外日は上書きしない。
- 新規TimeMarkシートに空の `SchoolEvents` と記載例の `SchoolEventsExample` が作成される。
- `SchoolEvents` だけを持つ旧シートURLも接続でき、端末バックアップを要求せず学校予定を読み込める。
- iPhone／iPadで「シートを開く」がスプレッドシート／DriveまたはSafariを起動しても、TimeMark画面を遷移させない。「シートから読み込み」「予定表を読み込み」「接続を解除」のアプリ内確認画面が表示・操作でき、Googleログイン後もTimeMark画面へ戻る。
- パッケージアプリの起動時、画面資産がアプリ内から読み込まれ、Service Worker登録失敗の警告が出ない。
- 端末内保存、JSONのエクスポート／インポート、Googleスプレッドシート同期・予定表読込が表示・操作できる。

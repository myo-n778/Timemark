# 回帰チェック

- LISTでドラッグ並び替え、右クリック編集、長押し編集が動く。
- Time Roadでドラッグ並び替えでき、LISTと同じ順序になる。
- 再読み込み後もターゲット順序が維持される。
- 設定画面のGoogle同期とJSON移行が表示・操作できる。
- `TimeMarkSchedule` の予定表読込が例外日へ反映される。
- パッケージアプリの起動時、画面資産がアプリ内から読み込まれ、Service Worker登録失敗の警告が出ない。
- 端末内保存、JSONのエクスポート／インポート、Googleスプレッドシート同期・予定表読込が表示・操作できる。
